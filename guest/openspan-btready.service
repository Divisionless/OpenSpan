[Unit]
Description=OpenSpan radio-ready boot helper (waits for radio, ensures bluez sees it)
After=bluetooth.service network.target

[Service]
Type=oneshot
ExecStart=/opt/openspan/btready.sh
RemainAfterExit=yes
TimeoutStartSec=200

[Install]
WantedBy=multi-user.target
