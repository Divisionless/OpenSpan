[Unit]
Description=OpenSpan Bluetooth audio keepalive (local hold stream)
After=openspan-audio.service
Requires=openspan-audio.service

[Service]
ExecStart=/opt/openspan/keepalive-audio.sh
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
