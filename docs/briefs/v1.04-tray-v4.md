# Brief v1.04 — Tray runtime: NOTIFYICON_VERSION_4 + NIF_SHOWTIP upgrade

You are upgrading the existing tray implementation in `win/openspan.py` (class
`TrayIcon` and the `_TRAY` registry, around lines 3874–4050). This is an
upgrade-in-place, NOT a rewrite. Read the whole block and its comments first —
every rule in them was paid for with a native crash.

## Reference

`D:\EsotericOS_Antigrav\src\EsotericOS.Platform.Windows\Shell\TrayIcon.cs` is
the working C# reference for V4 semantics. Port semantics, not syntax.

## Scope — exactly this, nothing else

1. **Opt into NOTIFYICON_VERSION_4** after every successful `NIM_ADD`:
   set `uVersion = 4` on the NID and call `Shell_NotifyIconW(NIM_SETVERSION=4, ...)`.
   Every NIM_ADD site must re-opt-in: initial add, the `ensure()` re-add path,
   and the `TaskbarCreated` re-add inside the WNDPROC.
2. **Add NIF_SHOWTIP (0x80)** to `uFlags` — under V4 the shell suppresses the
   standard tooltip unless explicitly requested (see the C# comment).
3. **Decode callbacks the V4 way**: the event arrives in `LOWORD(lParam)`
   (`evt = l & 0xFFFF`), icon id in HIWORD, anchor point in wParam. Map:
   - restore: `WM_LBUTTONUP` 0x0202, `WM_LBUTTONDBLCLK` 0x0203,
     `NIN_SELECT` 0x0400, `NIN_KEYSELECT` 0x0401 (keyboard activation)
   - menu: `WM_RBUTTONUP` 0x0205, `WM_CONTEXTMENU` 0x007B (keyboard menu key —
     accessibility, present in the C# reference)
4. Nothing else. Do not restructure the class, do not touch any other part of
   `openspan.py`, do not rename anything.

## Laws (violating any of these is a failed task)

- The window class + WNDPROC thunk stay IMMORTAL and registered once — do not
  touch the `_TRAY` lifetime rules.
- An exception must NEVER escape the WNDPROC (see its comment: 0xc000041d).
- No Tk calls anywhere in the WNDPROC path. The `on_restore`/`on_menu`
  callback contract is unchanged — the app marshals to Tk itself.
- Pure ctypes, no new dependencies, no new top-level imports.

## Deliverables

1. The edit to `win/openspan.py`.
2. `win/test_tray_v4.py` — structural checks in the house idiom (copy the
   `check(name, condition)` style from `win/test_brand.py`): NIM_SETVERSION
   follows every NIM_ADD site (all three), NIF_SHOWTIP is in the flags, the
   WNDPROC decodes `l & 0xFFFF`, WM_CONTEXTMENU and NIN_KEYSELECT are handled,
   the immortality comment block survives, no Tk identifier appears inside the
   WNDPROC function body.
3. `win/probe_tray_v4.py` — a live probe, runnable standalone: imports
   `TrayIcon` from `openspan`, creates one icon (tip "EsotericOS probe", icon
   `brand/esotericos-tray.ico`), pumps Win32 messages for ~2 seconds with a
   plain GetMessageW/PeekMessageW loop (no Tk), verifies NIM_ADD and
   NIM_SETVERSION both returned nonzero, then destroys the icon and exits 0.
   If importing `openspan` has side effects that make this unsafe, say so in
   your report instead of forcing it.

## Verify

Run `C:\Python313\python.exe win\test_tray_v4.py` and the existing suites
`win\test_brand.py` and `win\test_monitor_identity.py` — all must pass. Run
the probe too. If the sandbox blocks running python, still write everything
and say exactly what you could not run.

## Do not

- Do not commit, stage, or touch git in any way.
- Do not run, kill, or rebuild any .exe; do not restart explorer.
- Do not write outside `win/` in this repo.

## Report (your final message)

Test output verbatim, probe output verbatim, then one line per judgment call
you had to make. No pleasantries.
